const Navbar = () => {
  return (
    <nav className="navbar">
      <div className="navbar__brand">Dora</div>
      <div className="navbar__links">
        <a href="#">Dashboard</a>
        <a href="#">Discovery</a>
        <a href="#">Profile</a>
      </div>
    </nav>
  );
};

export default Navbar;
